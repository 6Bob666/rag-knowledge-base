# RAG 知识库问答系统 · 面试手册

> 本手册所有数字均来自仓库内的实际代码、`experiment_results/` 报告与 CI 测试结果，
> 可直接对照复查。面试时优先讲**取舍和证据**，不要堆名词。

## 0. 使用方式

| 场景 | 看哪一节 |
| --- | --- |
| 电话初面、自我介绍 | 第 1 节（2 分钟） |
| 技术面追问项目细节 | 第 2 节（5 分钟串讲）+ 第 5 节（问答） |
| 简历改项目描述 | 第 3 节 |
| 被问到指标 | 第 4 节（数字速查） |
| 被问到不会的 / 项目边界 | 第 6 节 |

---

## 1. 两分钟项目介绍

> 脚本按 300 字左右写，正常语速约 2 分钟。先背熟，再按面试官节奏删减。

我独立做了一个中文 RAG 知识库问答系统，用 FastAPI 实现，目标是完整走通
「文档入库 → 检索召回 → 精排 → 生成 → 评测」这条链路，并且每一步都要有可量化的证据。

检索层是混合检索：向量检索负责语义，BM25 负责关键词和专有名词，两者用 RRF 融合，
先扩召回 `top_k × 3` 个候选，再交给本地的 BGE Reranker 做交叉编码精排。
精排之后加了一个分数阈值，低于阈值的直接丢掉——如果全部被丢掉，系统就拒答，
用拒答换取低幻觉。

Agent 部分做了三层：Router 判断这个问题要不要查知识库；检索结果交给 Verifier
判断够不够用，分「无候选 / 低分 / 跑题 / 可用」四档；Planner 再按失败类型决定
是回原问题重搜还是换关键词重搜。整个循环由代码侧 `max_attempts` 硬限制，防止死循环。
另外还实现了标准的 Function Calling Tool Agent 和自建 MCP Server。

工程上，我比较看重的部分是可评测和可复现。除了自建的 24 题评测集，我还接入了
MTEB 的公开中文检索数据集 T2Retrieval，用 5000 条语料和 500 道带标注的 query
做了对照实验。同时做了 5 千 / 5 万 / 20 万三档规模的 HNSW 索引压测。
目前仓库有 240 项自动化测试，CI 不依赖任何外部 API。

---

## 2. 五分钟技术串讲

按下面五层讲，每层先给结论，再给证据。面试官打断追问也没关系，层与层之间是解耦的。

### 第 1 层：入库与幂等

```
上传文件
  → 计算 SHA-256 内容哈希
  → 按文件名加键控锁
  → 文本切分（chunk_size / chunk_overlap）
  → Embedding 向量化
  → 写 Chroma（向量 + 文本 + metadata）
  → SQLite 记录文档状态
```

两个设计点值得主动说：

1. **幂等判据是「文件名 + 内容哈希」，不是文件名。** 文件名可以重复，内容才代表同一份
   东西。同一份内容重复上传直接返回 `unchanged`，不做切分和向量化，省掉整条昂贵链路。
2. **替换版本时先写新、成功后再删旧。** SQLite 的 rollback 撤销不了 Chroma 的写入，
   两个系统不在同一个事务里，所以只能用「先保留旧版本 + 补偿」的方式保证可用性。

### 第 2 层：检索与精排

```
query
  → 向量检索（BGE）          取 top_k × 3
  → BM25（jieba 分词）        取 top_k × 3
  → RRF 融合                  1/(k + rank)，k=60
  → BGE Reranker 精排         CrossEncoder，「问题 + 文档」拼接
  → 阈值过滤                  低于阈值丢弃，全丢则拒答
```

为什么这么分工：

- 向量检索擅长语义相近但用词不同的情况；
- BM25 擅长缩写、型号、专有名词这类「必须字面命中」的场景；
- RRF 只看排名不看分数，天然规避了两路检索分数量纲不可比的问题；
- Reranker 是交叉编码器，把问题和文档拼起来一起编码，比双塔准得多，但慢得多，
  所以**只对候选池做，绝不扫全库**。

候选池为什么是 `top_k × 3`：这是一个召回和延迟的折中。Reranker 只能对已经召回的
文档排序，进不了候选池的文档永远补不回来，所以扩召回是必要的；但候选池越大，
精排耗时线性增长。

### 第 3 层：Agent 状态机

```
ROUTE → RETRIEVE → VERIFY → REPLAN → RETRIEVE → … → GENERATE → VALIDATE
```

Verifier 的四档结论：

| 结论 | 含义 |
| --- | --- |
| `empty` | 一个候选都没有，知识库里确实没有相关文档 |
| `low_score` | 有候选，但全被阈值拦掉 |
| `low_coverage` | 有文档，但关键术语覆盖率不足，内容跑题 |
| `sufficient` | 可以进入生成层 |

Planner 的补救策略：

| 失败类型 | 动作 |
| --- | --- |
| `empty` | 回原始问题重搜（查询改写可能把问题带偏了） |
| `low_score` + 完全没覆盖 | 换关键词查询（更短，更贴合 BM25） |
| 部分覆盖 | 不再重试，降级生成并标记 `degraded` |
| 没有预算 | 有文档就降级生成，没文档才拒答 |

三条防死循环的边界：检索次数只由 `max_attempts` 决定；Planner 只能返回白名单动作；
代码侧还有一层次数兜底，即使 Planner 有 bug 也突破不了上限。
`PLANNER_ENABLED=false` 可以一键退回旧行为。

### 第 4 层：Function Calling 与 MCP

**Function Calling Tool Agent**（`POST /chat/agent/tool`）和主链路的区别是：
主链路是代码决定「必须检索」，这条路是**模型自己决定要不要检索**。

走标准 tools 协议：`tools` 声明 → 模型返回 `tool_calls` → 代码执行 →
按 `tool_call_id` 用 `role=tool` 回填 → 循环直到模型不再要工具。

三个约束全部落在代码侧，不依赖模型自觉：

- 工具名白名单，未知工具直接返回错误给模型；
- 参数校验：模型给的 `arguments` 是 JSON 字符串，要解析并确认是对象、`query` 非空；
- `max_steps=3` 防死循环。

**MCP Server** 把知识库检索封装成标准工具，协议层是 JSON-RPC 2.0 over stdio，
支持 `initialize` / `ping` / `tools/list` / `tools/call`，暴露两个工具：
`search_knowledge_base` 和 `list_knowledge_documents`。

两者关系一句话：**MCP 管工具怎么被声明和复用，Function Calling 管模型怎么发起调用。**
我做的那层适配就是把 MCP 的 `inputSchema` 转成 Function Calling 的 `parameters`，
字段名不同、schema 内容一样。Agent 启动时先向 MCP 请求 `tools/list` 拿到「菜单」，
再把菜单翻译成模型能看懂的 `tools` 参数，这样新增工具只需要改 MCP Server。

### 第 5 层：评测与工程化

评测分两层：

- **检索层**：Recall@K、Precision@K、MRR、负样本拒答率；
- **生成层**：Faithfulness、Context Recall、Answer Relevance（LLM-as-judge，
  结构化输出 + 多次采样取多数，降低随机波动）。

两套数据集：

- 自建 24 题评测集（20 正样本 + 4 负样本），人工标注 `relevant_chunk_ids`；
- MTEB T2Retrieval 公开数据切片，5000 条语料 + 500 道带 qrels 的 query。

工程侧：依赖注入 + Fake 替身让 240 项测试完全不依赖真实模型和外部 API；
Docker 打包，模型只读挂载、数据走命名卷；`/health/live` 与 `/health/ready` 分离，
ready 只有在模型加载完成后才返回 200；Prometheus 兼容指标暴露请求数、状态码分布和 P95。

---

## 3. 简历项目描述

### 版本 A：一页简历精简版（3 行）

**RAG 智能知识库问答系统** · 个人项目

- **检索链路**：BGE 向量 + BM25 混合检索，RRF 融合后扩召回 `top_k×3`，BGE Reranker
  精排与阈值拒答；Verifier 分四档判定检索质量，Planner 按失败类型重搜或降级生成。
- **Agent 与协议**：规则/LLM 双模式 Router、Function Calling Tool Agent
  （工具白名单、参数校验、`max_steps`）、自建 MCP Server（JSON-RPC 2.0 + stdio）、
  按 `user_id` 隔离的长期记忆、SSE 流式输出。
- **评测与工程**：自建 24 题评测集 + MTEB T2Retrieval 公开数据 5000 语料/500 query
  对照实验；Reranker 把 Precision@3 从 0.781 提到 0.807、MRR 从 0.950 提到 0.960；
  完成 5千/5万/20万三档 HNSW 压测；240 项测试通过，Docker + CI + 健康检查。

### 版本 B：标准版（4 条，适合项目经历一栏）

**RAG 智能知识库问答系统**（FastAPI / ChromaDB / BGE / DeepSeek）· 个人项目

- 独立实现文档切分、向量化入库、混合检索、重排序到 LLM 生成的完整链路；
  上传接口以「文件名 + SHA-256」做幂等，按文件名键控锁保证并发安全。
- 检索采用向量 + BM25 双路召回、RRF 融合、`top_k×3` 扩召回后 Reranker 精排；
  引入分数阈值实现低分拒答，自建 24 题评测集上负样本拒答率 100%。
- 实现 Agentic RAG：Router 结构化决策、Verifier 四档验证、Planner 有界重搜，
  以及标准 Function Calling Tool Agent 和自建 MCP Server，工具白名单与参数校验
  全部落在代码侧。
- 建立分层评测与公开数据实验：接入 MTEB T2Retrieval（5000 语料 / 500 query），
  对比 Hybrid 与 Reranker 阈值策略；在 5千/5万/20万三档规模做 HNSW 参数压测，
  量化 `search_ef` 配错会让 Recall@10 掉到 0.455。

### 版本 C：面试口述版（讲证据，不讲形容词）

> 这个项目我最想讲的是「我把效果和性能都量化了」。
>
> 效果上，我准备了两套评测。自建 24 题用来做消融，能验证 Reranker 和阈值各自
> 的贡献；公开数据用 MTEB 的 T2Retrieval 切片，5000 条语料 500 道题，避免只在
> 自己造的小数据上自证。在 500 题上，Hybrid 的 Precision@3 是 0.781，
> 加 Reranker 之后是 0.807，MRR 从 0.950 到 0.960；如果阈值提到 0.5，
> Precision 能到 0.873，但 Recall 会从 0.966 掉到 0.880。
>
> 性能上，我用 GPU + FP16 + 批量推理把 Reranker 跑起来，500 题按题分摊的
> P95 大概是 696 毫秒；另外单独压了索引层，在 20 万规模下 HNSW 的 p50 稳定在
> 1.5 毫秒左右，而暴力检索涨到 11.6 毫秒。
>
> 这些数字让我能清楚说明每个参数是怎么选的，而不是凭感觉调。

---

## 4. 关键数字速查

### 自建 24 题评测集（`top_k=3`，threshold=0.5）

| 方案 | Recall@3 | Precision@3 | MRR | 负样本拒答率 |
| --- | --- | --- | --- | --- |
| Hybrid | 1.000 | 0.517 | 0.942 | 0.000 |
| Hybrid + Reranker | 1.000 | 0.842 | 1.000 | 1.000 |

### Agent Router 对照（24 题）

| 模式 | Recall@3 | Precision@3 | MRR | 负样本空返回率 | 平均路由耗时 | 平均总耗时 |
| --- | --- | --- | --- | --- | --- | --- |
| rules | 1.000 | 0.842 | 1.000 | 1.000 | 0.0 ms | 1125.4 ms |
| llm | 0.950 | 0.792 | 0.950 | 1.000 | 1183.3 ms | 2182.6 ms |

### MTEB T2Retrieval 500 题（5000 语料，`top_k=3`）

| 方案 | Recall@3 | Precision@3 | MRR |
| --- | --- | --- | --- |
| Hybrid | 0.966 | 0.781 | 0.950 |
| Hybrid + Reranker（阈值 0.0） | 0.966 | 0.807 | 0.960 |
| Hybrid + Reranker（阈值 0.5） | 0.880 | 0.873 | 0.879 |

### Verifier 校准（24 题）

| 场景 | 一致率 | 负样本误放行 | 触发重规划 |
| --- | --- | --- | --- |
| 阈值 0.5 + 覆盖度验证 | 1.000 | 0 / 4 | 0.167 |
| 阈值 0.5 + 不检查覆盖度 | 1.000 | 0 / 4 | 0.167 |
| 阈值 0.0 + 覆盖度验证 | 1.000 | 2 / 4 | 0.083 |
| 阈值 0.0 + 不检查覆盖度 | 1.000 | 4 / 4 | 0.000 |

### 索引压测（`intrinsic_dim=32`，`top_k=10`）

| 规模 | 配置 | Recall@10 | p50 | p95 |
| --- | --- | --- | --- | --- |
| 5 千 | `hnsw_default` | 1.000 | 1.21 ms | 3.53 ms |
| 20 万 | `hnsw_default` | 0.950 | 1.54 ms | 3.21 ms |
| 20 万 | `hnsw_small_ef` | 0.455 | 1.24 ms | 1.62 ms |
| 20 万 | `hnsw_high_recall` | 1.000 | 2.66 ms | 3.19 ms |
| 20 万 | `exact_numpy` | 1.000 | 11.59 ms | 12.04 ms |

### 其他

- 自动化测试：**240 passed**，2 个第三方库 DeprecationWarning；
- 生成层（5 正样本、3 次采样取多数）：Faithfulness 0.800、Context Recall 0.800、
  Answer Relevance 1.000；
- Reranker 批量推理（batch=4，RTX 4060 + FP16）：批次实际平均 1218.9 ms、
  P95 2728.2 ms；按题分摊后总耗时 P95 约 695.8 ms。

---

## 5. 高频追问与参考回答

### 5.1 选型与架构

**Q：为什么用 RAG，不直接微调模型？**

这个场景的瓶颈不是「模型不懂语言」，而是「模型不知道我的私有文档」。RAG 把知识
放在外部库里，更新文档只需要重新入库，不用重训；同时检索结果可以带引用，答案可追溯。
微调更适合改变输出风格、格式或领域推理习惯，不适合承载频繁变更的事实性知识。
两者也不互斥——真实系统里常见做法是微调对齐风格，知识仍然走 RAG。

**Q：为什么用 Chroma 而不是 FAISS 或 Milvus？**

Chroma 自带持久化、metadata 过滤和 ID 管理，开发期可以直接当「带 metadata 的
本地向量库」用，省掉自己维护 ID 映射和过滤逻辑的成本。FAISS 更偏底层索引库，
元数据和持久化要自己写；Milvus 适合更大规模和分布式部署，但对这个项目是过度设计。
我也用压测量化了边界：20 万规模内暴力检索还能打（11.6 ms、召回 100%），
继续变大或需要多副本、并发写入时才值得换 Milvus/FAISS。

**Q：为什么用 SQLite 存文档状态，而不是全放 Chroma？**

职责不同。SQLite 管「业务状态」——文件名、内容哈希、processing/indexed/failed，
这是需要事务和唯一约束的关系型数据；Chroma 管「向量和文本片段」。
另外两者是两个独立系统，SQLite 的 rollback 撤销不了 Chroma 的写入，这一点直接
决定了替换版本时必须用「先写新、成功后再删旧」的补偿策略。

### 5.2 检索层

**Q：Embedding 检索和 BM25 各自解决什么问题？**

Embedding 解决语义相似但用词不同的情况，比如「怎么退货」和「退款流程」；
BM25 解决必须字面命中的情况，比如缩写、型号、专有名词、错误代码。
这类 token 在语义空间里往往离得很近，容易被淹掉，但 BM25 能精准抓住。

**Q：RRF 怎么算？为什么用 RRF 而不是加权分数？**

RRF 是 `score(d) = Σ 1/(k + rank_i(d))`，`k` 一般取 60。
关键是它**只用排名，不用原始分数**。向量检索给的是余弦相似度，
BM25 给的是无上界的关键词分数，两者量纲完全不可比，归一化也很脆弱；
RRF 直接绕过了这个问题，而且对某一路的分数异常天然鲁棒。

**Q：为什么候选数是 `top_k × 3`？**

两个原因。Reranker 只能对已召回的文档排序，进不了候选池的文档永远补不回来，
所以必须扩召回；但候选池越大，精排耗时线性增长。3 倍是在这个项目的数据规模上
实测下来召回和延迟比较平衡的值，它不是理论最优，是一个需要按数据规模调的参数。

**Q：Reranker 为什么不能扫描整个知识库？**

Reranker 是交叉编码器，要把「问题 + 文档」拼成一条输入完整过一遍模型。
双塔结构可以预计算文档向量、查询时只算一次，交叉编码器做不到——它在查询时
对每个候选都要重新前向一次。所以复杂度是 O(候选数) 的模型推理，只能对候选池做。

**Q：阈值为什么可能降低 Recall？**

阈值的作用是过滤低分文档。分数是连续的，而「相关」和「不相关」之间没有天然边界。
阈值提高，一些本来包含答案但表述不典型的文档会被误杀，这些文档一旦被丢掉，
后面的链路没有任何补救手段。我在 500 题上实测到了这个代价：阈值从 0.0 提到 0.5，
Precision 从 0.807 升到 0.873，但 Recall 从 0.966 掉到 0.880，MRR 从 0.960 掉到 0.879。
所以阈值要按业务目标选：宁可拒答就调高，宁可不漏就调低。

**Q：`low_score` 和 `low_coverage` 的区别？**

`low_score` 是「有候选，但都被阈值拦掉了」，说明检索确实找到了一些东西，
只是置信度不够；`low_coverage` 是「有文档通过了阈值，但问题里的关键术语
在文档里覆盖率不足」，说明召回的内容跑题了。前者更像是阈值卡得太严，
后者更像查询本身写偏了。两者的补救动作不同：前者换关键词查询，
后者回到原始问题重搜。

### 5.3 Agent 与协议

**Q：Router、Planner、Verifier 分别负责什么？**

Router 在最前面，判断这个问题要不要查知识库，输出 `retrieve` / `direct_answer` /
`refuse` 三档决策。Verifier 在检索之后，判断这一轮结果能不能支撑回答，
分四档给结论。Planner 在 Verifier 判定不够用时决定怎么补：回原问题还是换关键词。
一句话：Router 决定「要不要搜」，Verifier 判断「搜够没有」，Planner 决定「怎么再搜」。

**Q：为什么 Router 要有规则版和 LLM 版两套？**

LLM Router 更灵活，能理解复杂意图，但会犯错也不便宜。我在 24 题上做了对照：
LLM 版把「CNN 的全称是什么」误判成「常识性事实，无需检索」，绕过知识库直接回答，
Recall 从 1.0 掉到 0.95，平均总耗时从 1125 ms 涨到 2183 ms。
所以现在的设计是规则兜底优先、LLM 决策可选，且 LLM 输出解析失败会自动回退规则版，
Router 本身抛异常也会安全回退成「检索」。失败的默认动作要选偏保守的那一侧。

**Q：Function Calling 和 MCP 有什么区别？**

Function Calling 解决「模型怎么发起一次工具调用」，是模型侧的交互协议；
MCP 解决「工具怎么被声明、分发和复用」，是系统侧的集成协议。
在项目里，Agent 启动时先向 MCP Server 请求 `tools/list` 拿到工具声明，
再把这套声明翻译成模型需要的 `tools` 参数；模型决定调用后，Agent 再通过
`tools/call` 把请求转回 MCP 执行。这样新增一个工具只需要改 MCP Server，
不用同时改 Agent 的 prompt 和代码。

**Q：`tools → tool_calls → role=tool` 这个协议是你自己写的吗？**

不是。这是 OpenAI 兼容接口的标准协议，DeepSeek 也遵循它。我实现的是**服务端这一侧
的处理逻辑**：声明 `tools`、解析模型返回的 `tool_calls`、执行工具、
再按 `tool_call_id` 用 `role=tool` 把结果回填进消息列表，循环到模型不再调用工具为止。
这套流程的边界条件才是真正要写的部分——工具白名单、参数是 JSON 字符串要解析、
参数非法不能崩、循环必须有 `max_steps` 上限。

**Q：为什么最大调用次数必须由代码强制，而不是让 LLM 自觉？**

因为 prompt 是概率性的约束，代码是确定性的约束。模型可能因为上下文污染、
工具返回异常、或者纯粹的采样波动而反复调用。如果没有硬上限，
一次请求可能耗尽 token 预算甚至拖垮服务。所以 `max_steps` 写在循环条件里，
Planner 的动作也限定在白名单内，再加一层代码侧次数兜底——
即使模型或 Planner 出问题，也突破不了上限。

### 5.4 多轮与记忆

**Q：查询改写和知识库检索的关系是什么？**

查询改写解决的是「用户问题本身不适合直接检索」，典型场景是指代消解。
比如多轮里用户先问 CNN，再问「它有什么应用」，直接拿「它有什么应用」去检索
什么都召不回来；改写后变成「CNN 有什么应用」才可检索。
改写结果**只用于检索**，最终回答时用户看到的仍然是原问题。
如果改写后仍然检索不到可靠文档，系统应该拒答，而不是让 LLM 猜。

**Q：长期记忆为什么只从用户话语抽取，不从模型回答抽取？**

因为模型回答可能包含幻觉。如果从 assistant 消息里抽取「用户偏好」或「事实」，
等于把幻觉固化成长期记忆，之后每一轮都会被这个错误污染，而且很难发现和回滚。
记忆的职责是记录用户明确说过的信息，不是记录模型推断出的信息。

### 5.5 评测

**Q：为什么不能只看最终答案来评价 RAG？**

因为 RAG 是多阶段的，最终答案错了可能是检索没召回到、精排排错了、
上下文被截断了，或者 LLM 自己没用好上下文。只看最终答案无法定位。
所以我把评测分层：检索层算 Recall/Precision/MRR，生成层算
Faithfulness/Context Recall/Answer Relevance。这样能明确回答
「是检索层的锅还是生成层的锅」。

**Q：Recall、Precision、MRR 分别衡量什么？**

Recall@K 是「前 K 个结果里有没有至少一个正确的」，衡量有没有捞到；
Precision@K 是「前 K 个结果里有多少是正确的」，衡量捞回来的干不干净；
MRR 是第一个正确结果的排名的倒数，衡量排序好不好。
典型的取舍是：Recall@5 高但 Recall@1 低，说明答案在候选里但排序不好——
这正是 Reranker 要解决的问题。

**Q：为什么自建评测集只有 24 题，还要再接公开数据集？**

24 题规模太小，而且题目是我自己根据知识库内容写的，存在明显的自证偏差：
知识库只有 77 个 chunk，题目和内容高度相关，Recall 天然偏高。
所以我又接了 MTEB 的 T2Retrieval 公开切片——5000 条语料、500 道带官方 qrels
的 query，标注不是我定的。两套数据的分工是：小数据集做快速消融和参数校准，
公开数据集做规模化验证。这两套结果的差距本身就是一个值得讲的点。

**Q：为什么实验报告要记录知识库版本和评测集版本？**

否则指标变化无法归因。如果两次实验的 Recall 不同，可能是系统改好了，
也可能是知识库新增了文档、chunk 重新切分导致旧的 `relevant_chunk_ids` 失效、
或者评测集本身改了。所以报告里保存配置快照——数据集、collection、
`top_k`、阈值、chunk 参数、模型名、批大小、时间戳。
现在 `experiment_runner.py compare` 能直接把这些报告横向拉成一张表。

**Q：Reranker 的效果你怎么证明的？**

用同一批题、同一个语料做对照，只改一个变量。在 500 题上，Hybrid 的
Precision@3 是 0.781、MRR 0.950；加 Reranker（阈值 0.0）后是 0.807 和 0.960，
Recall 保持 0.966 不变。这说明 Reranker 的贡献是**改善排序和结果纯度**，
而不是扩大召回——这也符合原理，它只能重排已召回的候选。

### 5.6 工程与性能

**Q：为什么要用依赖注入和 Fake 替身？**

因为真实的 Embedding、Reranker 和 LLM 调用都很慢、要网络、要 API Key，
还带随机性。如果测试直接调用它们，测试会变得又慢又不稳定，CI 里也没法跑。
通过 `Depends(get_store)` 这类依赖注入，测试里用
`app.dependency_overrides[get_store] = lambda: FakeStore()` 就能把真实模型换成替身，
几分钟内跑完 240 项测试。需要注意：真正起作用的是**依赖覆盖**，
不是 `Depends` 本身——`Depends(get_reranker)` 默认还是会加载真实模型。

**Q：SQLite 的 rollback 为什么撤销不了 Chroma 的写入？**

因为它们是两个独立的存储系统，各自管理自己的事务，没有分布式事务协调者。
SQLite 回滚只影响 SQLite 自己的事务日志，Chroma 那边已经落盘的向量不会回滚。
所以跨系统的写入只能靠应用层的补偿设计：先写新版本、确认成功之后再删旧版本，
这样中途失败时旧数据仍然可用，最坏情况只是留下冗余数据，而不是丢数据。

**Q：SSE 和 WebSocket 怎么选？**

这个场景是「服务端持续向客户端推送模型生成的 token」，是单向流，
SSE 够用且更简单：基于普通 HTTP，自带重连语义，不需要维护双向连接状态。
WebSocket 适合需要双向实时交互的场景，比如协同编辑、实时语音。
项目里流式接口按 SSE 的事件格式返回 `start` / `token` / `done` / `error`，
前端靠 `done` 判断生成结束，靠 `error` 处理「HTTP 已经 200 但生成中途失败」的情况。

**Q：流式输出怎么做安全检查？**

这是个真实的矛盾：流式要求边生成边发，但安全检查需要看完整文本才能判断。
发现后面内容有问题时，前面已经发给客户端了，撤不回来。
我当前的取舍是安全优先——先完整生成、过一遍 `validate_answer`，
再按 SSE 格式分段推送。代价是首 token 延迟变高，收益是不会把违规内容发出去。
如果更看重体验，可以做增量检查加缓冲窗口，但逻辑复杂度会明显上升。

**Q：你的索引压测得出什么结论？**

四条。第一，ANN 的价值是延迟不随规模增长——HNSW 从 5 千到 20 万，
p50 稳定在 1.2~1.6 ms，而暴力检索从 0.18 ms 涨到 11.59 ms，因为它严格 O(N)。
第二，参数配错比不建索引更危险——`search_ef` 设成等于 `top_k` 时，
20 万规模召回只有 0.455，一半以上的正确答案根本没进候选池，而且它看起来还更快，
因为它几乎没搜索。第三，漏召回不可逆，Reranker 只能重排已召回的文档，
所以 ANN 的 Recall 是整条链路的上限。第四，20 万以内暴力检索仍然能打，
真该换 Milvus/FAISS 的时机是规模继续上升或需要多副本并发写入。

**Q：压测为什么用合成向量，不用真实语料？**

因为只有合成数据能控制「结构有多难」。最关键的变量是有效语义维度
`intrinsic_dim`：真实 embedding 的内在维度通常远低于向量维度，近邻分得开；
满维随机数据的近邻分数几乎相同，是 ANN 的最坏情况。
同一份代码，`intrinsic_dim=32` 时 20 万规模召回 0.950，
`intrinsic_dim=512` 时掉到 0.230。所以报告必须带这个参数，否则数字没有意义。

---

## 6. 边界、不足与不会的问题怎么答

面试官问到边界时，如实说比硬撑更加分。下面这些是项目的真实限制：

| 限制 | 说明 |
| --- | --- |
| Docker 未在本机实测 | 本机没有可用的 Docker 环境，只完成了 Compose 配置、YAML 解析和本地 Uvicorn 服务验证 |
| 公开数据集全是正样本 | T2Retrieval 切片 500 题都有标准答案，因此不能用来评价拒答率 |
| 小知识库的指标偏高 | 自建知识库只有 77 个 chunk，题目与内容高度相关，Recall 偏高，不代表生产表现 |
| Reranker 延迟不是生产基线 | 1218.9 ms 是 Windows 单卡、batch=4 的实测批次耗时，按题分摊后才约 696 ms |
| LLM Router 有误判 | 24 题对照实验里出现过把知识库事实判成常识的情况 |
| 单机存储 | SQLite/Chroma 都在本机，未验证多实例并发写入 |
| 生成层样本量小 | 生成层评测只跑了 5 道正样本、3 次采样，只能验证链路不能下结论 |

被问到完全不会的问题时，用这个结构回答：

> 这块我还没做过。我的理解是……（说出你掌握的相邻知识），
> 如果要做的话，我会先确认……（说出关键约束），
> 这个项目里它对应的是……（如果有对应关系就说）。

不要编造指标，也不要编造经历。面试官通常能分辨出来，而「知道自己不知道什么」
本身就是被考察的能力。

---

## 7. 开场前的三条提醒

1. **先讲取舍，再讲功能。** 「我选了 A 而不是 B，因为在这个数据规模下……」
   比「我用过 A、B、C」有说服力得多。
2. **数字要能追到来源。** 本手册所有数字都能在 `README.md`、
   `experiment_results/` 和 `pytest` 输出里找到，被追问时直接说文件位置。
3. **主动说边界。** 说清楚「这个结论在什么条件下成立」，比把结论说得更大更可信。
